#Run this scrip in the case of fresh install of R

install.packages(c("dplyr",
                   "dslabs",
                   "kernlab",
                   "tidyverse"
                   )
                 )

